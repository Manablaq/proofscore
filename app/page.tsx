// Keep the established ProofScore experience at the published project URL.
// The v10 contract work remains isolated at /v10 while its verification flow
// is incorporated into this interface without replacing the product surface.
export { default } from './v9/page'
